# Development Session Handoff

**Session file:** `2026-08-19_1555_codex_P1-09.md`
**Owner/agent:** `codex/root` with `codex/p1-09`
**Primary task:** `P1-09`
**Secondary tasks:** `P1-23` selected-runtime verification
**Started:** `2026-08-19T15:33:00+07:00`
**Ended:** `2026-08-19T15:55:00+07:00`
**Outcome:** Completed

## Intent

Complete the P1-09 PostgreSQL idempotency, transactional outbox, and retry
conventions only after the selected Prisma test runtime proves duplicate
commands, durable delivery evidence, expired-lease recovery, and replay-safe
workers. Do not reset, drop, recreate, or otherwise destructively change the
approved database or its Secret Manager connection.

## Work Completed

- Kept P1-09's deployed base migration immutable and retained its two applied
  follow-up migrations for `ABANDONED` / `LEASE_EXPIRED` delivery evidence and
  the deferred outbox integrity trigger.
- Made the focused P1-09 test use an epoch clock, isolating its claims from
  persistent current-time outbox rows without changing production claim
  semantics.
- Inspected `_prisma_migrations` and PostgreSQL enum metadata after a duplicate
  migration attempt failed. The original `20260819150000` and
  `20260819150100` migrations had already succeeded; `20260819170000` failed
  before applying its sole `ALTER TYPE` statement because `ABANDONED` already
  existed.
- Restored the original, already-applied migration directory identities and
  marked only the zero-step duplicate `20260819170000_p1_09_outbox_abandoned_lease`
  record rolled back. Prisma then reported the schema up to date with no
  pending migrations.
- Removed the temporary sanitized failure-diagnostic output from the P1-23
  runner before the final image build.
- Built, inspected, deployed, and ran a new Cloud Run verifier image. The full
  suite passed, including the P1-09 focused integration test and structural
  verifier.

## Files Changed

| Path | Change and reason |
| --- | --- |
| `tests/integration/p1-09-reliability.test.ts` | Use a deterministic historical fixture clock so existing current-time persistent messages cannot affect claim order. |
| `prisma/schema.prisma` | Add `ABANDONED` to the durable outbox-delivery outcome contract. |
| `prisma/migrations/20260819150000_p1_09_outbox_abandoned_lease/migration.sql` | Persist the `ABANDONED` enum outcome; already applied before this session's duplicate-recovery inspection. |
| `prisma/migrations/20260819150100_p1_09_outbox_abandoned_lease_enforcement/migration.sql` | Enforce immutable abandoned-lease evidence before reclaim through check and deferred trigger rules; already applied before duplicate-recovery inspection. |
| `src/lib/reliability/core.ts` | Append immutable `ABANDONED` / `LEASE_EXPIRED` evidence atomically before an expired lease is reclaimed. |
| `scripts/verify-p1-09.mjs` | Verify P1-09 structural constraints and trigger contract. |
| `deploy/p1-23/run-verification.mjs` | Remove temporary sanitized diagnostic logging after focused troubleshooting concluded. |
| `DEVELOPMENT-CHECKLIST.md` | Mark P1-09 done and release the exclusive Active Work row. |
| `docs/progress/sessions/2026-08-19_1555_codex_P1-09.md` | Durable final verification and recovery evidence. |

## Verification

| Command/check | Result | Evidence or relevant output |
| --- | --- | --- |
| Read-only `_prisma_migrations` and `pg_enum` inspection | PASS | `20260819150000_p1_09_outbox_abandoned_lease` and `20260819150100_p1_09_outbox_abandoned_lease_enforcement` both had non-null `finished_at`; the failed `20260819170000` record had null `finished_at` and SQLSTATE `42710`; enum labels already included `ABANDONED`. |
| `npx prisma migrate resolve --rolled-back 20260819170000_p1_09_outbox_abandoned_lease` | PASS | Safe because the failed duplicate migration contained one `ALTER TYPE ... ADD VALUE`, PostgreSQL rejected it before a change, and the original applied migration had already added the enum label. |
| `npx prisma migrate status` | PASS | `11 migrations found in prisma/migrations`; `Database schema is up to date!` |
| `npm run db:migrate:test` | PASS | Test selector validation passed and Prisma reported `No pending migrations to apply.` |
| `npx prisma validate`, `npx prisma generate`, `npx tsc --noEmit`, `npm run test:p1-23`, `git diff --check` | PASS | Schema/client/typecheck and all four P1-23 selector tests pass; no whitespace errors (only existing CRLF warnings). |
| Cloud Build image provenance | PASS | Build `81c0420f-baa4-435c-b27d-96e74ae7ed73`; tag `20260819154653`; immutable digest `sha256:51a7e48f6c08fbe96cdf7e7ccdadcbec89aab07b8c3faeb0b1c6b7d4aa610f40`. Extracted image contains `ABANDONED`, the original applied P1-09 migration identities, the enforcement trigger, and zero `DIAGNOSTIC` markers. |
| Cloud Run Job readback | PASS | Generation 20 uses the digest above. `DATABASE_URL` remains Secret Manager `SPMNT-ACC-AUDIT_PRISMA-POSTGRES-URI:latest`; `APP_ENV=test`, `NODE_ENV=test`, `SCHOOL_SEED_ESAO_CODE=1000960001`, `TEST_DATABASE_ID=db_cmsywhp011xf62pdt4pmp50xk`, and `TEST_DATABASE_SECRET_ID=SPMNT-ACC-AUDIT_PRISMA-POSTGRES-URI` remain unchanged. |
| `gcloud run jobs execute p1-23-db-verifier --region=asia-southeast3 --project=spmnt-sch-acc-audit --wait` | PASS | Execution `p1-23-db-verifier-mk74w` completed successfully in 36.06 seconds with `succeededCount=1`. |
| Ordered final container result lines | PASS | `CHECK=CONFIG;RESULT=PASS`; `CHECK=MIGRATE;RESULT=PASS`; `CHECK=SEED;RESULT=PASS`; `CHECK=P1_03_VERIFY;RESULT=PASS`; `CHECK=P1_07_TEST;RESULT=PASS`; `CHECK=P1_07_VERIFY;RESULT=PASS`; `CHECK=P1_08_TEST;RESULT=PASS`; `CHECK=P1_08_VERIFY;RESULT=PASS`; `CHECK=P1_22_TEST;RESULT=PASS`; `CHECK=P1_09_TEST;RESULT=PASS`; `CHECK=P1_09_VERIFY;RESULT=PASS`; `CHECK=P1_17_TEST;RESULT=PASS`; `CHECK=P1_17_VERIFY;RESULT=PASS`; `VERIFY_RUN=PASS`; container exit `0`. |

## Domain and Architecture Decisions

- An expired worker lease is immutable operational evidence. Reclaim atomically
  appends `ABANDONED` with `LEASE_EXPIRED` before the replacement worker lease;
  it does not rewrite a prior attempt.
- The selected Prisma database remains identified by the approved selector pair,
  not by a PostgreSQL URL pathname heuristic. Its `/postgres` connection
  pathname and Secret Manager value remain unchanged.

## Blockers and Risks

None for P1-09. Final selected-runtime execution emitted no `P3009`, `P3018`,
`P1001`, or PostgreSQL error. The database was not reset, dropped, recreated,
or destructively modified; no Secret Manager value or Cloud Run environment
setting changed.

## Checklist Updates

- Task status changes: `P1-09` `[ACTIVE]` -> `[DONE]`.
- New task IDs: None.
- Active Work row released/updated: Removed after durable final verification.

## Next Exact Action

For `P1-10`, reassess its stale `BLK-015` runtime dependency against the
passing selected-runtime P1-09 contract, then claim only its transactional
scoped-number allocator files if its remaining dependencies are satisfied.
