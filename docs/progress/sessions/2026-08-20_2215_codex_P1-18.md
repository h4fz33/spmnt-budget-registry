# Development Session Handoff

**Session file:** `2026-08-20_2215_codex_P1-18.md`
**Owner/agent:** `codex/p1-18`
**Primary task:** `P1-18`
**Secondary tasks:** None
**Started:** `2026-08-20T21:35:00+07:00`
**Ended:** `2026-08-20T22:15:00+07:00`
**Outcome:** Blocked

## Intent

Implement the bounded `P1-18` `AUTH-06` path: a fresh-authenticated System
Admin may technically apply or revoke only an exact Private Business / Product
Owner-approved synthetic ESAO Admin configuration for the fixed 17-School
pilot scope. The capability must remain distinct from School roles and from
any external governmental appointment.

## Work Completed

- Added a dedicated ESAO Admin configuration, fixed School-scope, and
  immutable Product Owner/executor provenance model with restrictive foreign
  keys, active-subject uniqueness, source/role/evidence checks, and deferred
  shape assertions.
- Added the `AUTH-06` service with fresh System Admin authentication, exact
  active synthetic subject validation, fixed active 17-School scope, immutable
  product approval evidence, self-configuration and replay denial, and
  immediate identity/membership authorization-version invalidation on
  revocation.
- Added a resolver that grants only the dedicated ESAO Admin capability and
  fails closed for an identity with any other membership or School role.
- Added focused integration coverage for missing/altered evidence, unsupported
  capability, self-configuration, stale authentication, replay, revocation,
  resolver invalidation, and mutation guards on configuration, scope, and
  provenance records.
- Added the structural verifier and P1-23 runner wiring. The runtime evidence
  fields are explicit and synthetic-only; they preserve accountable Product
  Owner approval separately from System Admin technical execution.
- Tightened the deferred provenance assertion so a configuration transaction
  cannot commit without its matching `AUTH-06` execution audit event.

## Files Changed

| Path | Change and reason |
| --- | --- |
| `prisma/schema.prisma` | Add the dedicated P1-18 capability, scope, and provenance models. |
| `prisma/migrations/20260819200000_p1_18_esao_admin_configuration/migration.sql` | Add database constraints, deferred integrity checks, and immutable mutation guards. |
| `src/lib/authorization/esao-admin.ts` | Implement exact `AUTH-06` configuration/revocation and resolver behavior. |
| `tests/integration/p1-18-esao-admin.test.ts` | Cover authorization, scope, replay, revocation, and trigger behavior. |
| `scripts/verify-p1-18.mjs` | Verify durable database structure and separate approval/execution evidence. |
| `package.json` | Add focused P1-18 test and verifier commands. |
| `deploy/p1-23/run-verification.mjs` | Add ordered P1-18 test and verifier checks to the controlled runner. |
| `docs/progress/sessions/2026-08-20_2215_codex_P1-18.md` | Record implementation and current verification state. |

## Verification

| Command/check | Result | Evidence or relevant output |
| --- | --- | --- |
| `npx prisma format --check` | PASS | Prisma schema formatting check passed. |
| `npx prisma validate` | PASS | Schema is valid. |
| `npx prisma generate` | PASS | Prisma Client 7.9.1 generated successfully. |
| `npx tsc --noEmit` | PASS | TypeScript completed with no diagnostics. |
| `git diff --check` | PASS | No whitespace errors; existing CRLF warnings only. |
| `npm run db:migrate:test` against approved hosted synthetic runtime | BLOCKED | Prisma returned `P1001`; the database host was unreachable. |
| Isolated local synthetic migration replay | BLOCKED | The sandbox denied Prisma schema-engine spawn (`EPERM`); the requested elevated execution was rejected by the platform before execution. No direct-SQL workaround was used. |
| `npm run test:p1-18` / `npm run verify:p1-18` | NOT RUN | Require a successfully migrated synthetic database. |

## Domain and Architecture Decisions

- `EsaoAdminConfiguration` is an application capability record, not a
  `SchoolRoleAssignment`, generic grant, or external governmental appointment.
- The exact active synthetic subject must have only one active ESAO membership
  and no School role. This prevents the `AUTH-06` path from being reused as an
  ordinary membership or School-role route.
- Revocation is the only permitted configuration update; it preserves subject,
  scope, source, integrity digest, effective time, and creation time, then
  invalidates the subject's authorization versions immediately.
- The Product Owner user authorization on 2026-08-20 permits implementation of
  this synthetic-only contract. Each runtime configuration/revocation still
  requires its own attributable evidence record and cannot infer a real-world
  appointment.

## Blockers and Risks

- P1-18 cannot be marked done until the migration, focused integration test,
  and structural verifier run against an isolated synthetic PostgreSQL runtime.
- The approved hosted synthetic runtime was unreachable. A local disposable
  runtime exists, but the platform rejected the elevated Prisma execution
  needed to replay migrations. The local database was created only for this
  verification attempt and contains no applied P1-18 migration.
- No production, real School, or personal data was used or authorized.

## Checklist Updates

- Task status: `P1-18` `[ACTIVE]` -> `[BLOCKED]` pending executable evidence.
- New task IDs: None.
- Active Work row released: Yes, after static validation.

## Next Exact Action

Restore access to the approved synthetic PostgreSQL runtime, then run `npm run
db:migrate:test`, `npm run test:p1-18`, and `npm run verify:p1-18` before the
root checklist owner releases or completes `P1-18`.
