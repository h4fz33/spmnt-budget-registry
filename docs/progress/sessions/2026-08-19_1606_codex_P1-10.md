# Development Session Handoff

**Session file:** `2026-08-19_1606_codex_P1-10.md`
**Owner/agent:** `codex/root`
**Primary task:** `P1-10`
**Secondary tasks:** P1-23 selected-runtime verification
**Started:** `2026-08-19T16:06:33+07:00`
**Ended:** `2026-08-19T18:07:33+07:00`
**Outcome:** Completed

## Intent

Complete durable transaction-safe scoped numbering without modifying signed
P0-09 artifacts. The acceptance condition requires exact money/date/reference
primitives and an idempotent, serializable, School/fiscal-year/register scoped
allocator that does not leave committed gaps after rollback.

## Work Completed

- Kept the P1-10 durable sequence and append-only allocation-ledger migration
  as the allocator's database boundary. Deferred consistency triggers require
  `nextValue = allocation count + 1` and contiguous allocation values, while
  allowing a newly created empty scope at `nextValue = 1`.
- Confirmed the allocator replays its P1-09 idempotency record, allocates
  concurrent first-scope numbers safely, and rolls back a caller transaction
  without retaining a sequence, allocation, or business record.
- Corrected the integration test to accept an empty initial sequence, which is
  valid under the migration's stated invariant, while retaining forged advance
  and direct-allocation rejection coverage.
- Corrected the structural verifier to check PostgreSQL's actual 63-byte
  truncated unique-constraint identifier and all expected restrictive foreign
  keys.
- Built Cloud Build `63811b09-48a2-44ef-a103-2ca337a0ba3e`, tag
  `20260819175548`, immutable digest
  `sha256:771e1fa64c8c4f55434705bdf64e3f8449425c0ec95134373ac8cc7564f77164`.
  Updated only the P1-23 Job image, then ran the selected verifier.
- Recorded `P1-24` separately after a disposable fresh local database exposed
  the P1-09 lexical migration-order defect. This was not folded into P1-10.

## Files Changed

| Path | Change and reason |
| --- | --- |
| `tests/integration/p1-10-scoped-numbering.test.ts` | Accept a valid empty initial scope at `nextValue=1`; retain allocation-ledger tamper rejection coverage. |
| `scripts/verify-p1-10.mjs` | Verify PostgreSQL's actual truncated unique-constraint catalog name plus the restrictive FK contract. |
| `DEVELOPMENT-CHECKLIST.md` | Complete/release P1-10 and create scoped P1-24 clean-migration follow-up work. |
| `docs/progress/sessions/2026-08-19_1606_codex_P1-10.md` | Durable final verification evidence. |

## Verification

| Command/check | Result | Evidence or relevant output |
| --- | --- | --- |
| `npm run test:p1-10` | PASS | 4/4 exact money, fiscal-date, reference, and scoped-number primitive tests passed. |
| Fresh disposable PostgreSQL replay with the P1-10 schema contract | PASS | Focused integration test passed 2/2 for idempotent replay/concurrency, rollback, School scope, and ledger protections. |
| `npm run verify:p1-10` against the disposable replay database | PASS | Tables, constraints, triggers, and restrictive foreign keys verified. |
| `npx prisma validate`, `npx prisma generate`, `npx tsc --noEmit`, `git diff --check` | PASS | Schema, generated client, TypeScript, and whitespace checks passed. |
| Cloud Build provenance | PASS | Build `63811b09-48a2-44ef-a103-2ca337a0ba3e`, tag `20260819175548`, digest `sha256:771e1fa64c8c4f55434705bdf64e3f8449425c0ec95134373ac8cc7564f77164`. |
| Cloud Run Job configuration readback | PASS | Only the image changed. `DATABASE_URL` remains Secret Manager `SPMNT-ACC-AUDIT_PRISMA-POSTGRES-URI:latest`; selected database ID, selector secret ID, test environment, service account, timeout, and resource limits are unchanged. |
| `gcloud run jobs execute p1-23-db-verifier --region=asia-southeast3 --project=spmnt-sch-acc-audit --wait` | PASS | Execution `p1-23-db-verifier-9wqhr` completed in 42.08 seconds with `succeededCount=1`. |
| Ordered selected-runtime result lines | PASS | `P1_10_UNIT`, `P1_10_INTEGRATION`, and `P1_10_VERIFY` all passed; the full controlled run ended `VERIFY_RUN=PASS` and container exit `0`. |

## Domain and Architecture Decisions

- An untouched scoped-number sequence is a valid state: zero allocations and
  `nextValue=1`. The allocation ledger becomes mandatory only when a value is
  allocated, and the deferred trigger then prevents gaps, duplicates, or forged
  advancement at commit.
- The P0-09 fiscal-period inconsistency remains `P1-10A` / `BLK-013`; no
  signed fixture, hash, expected result, or Product Owner approval was changed.

## Blockers and Risks

- `P1-24` is required before Phase 1 approval: a pristine local database using
  standard lexical Prisma deployment order fails because P1-09 follow-up
  migrations precede the migration that creates `OutboxDeliveryOutcome`. The
  selected synthetic database remains migrated and the P1-10 selected-runtime
  evidence above is valid; no reset, drop, recreation, secret mutation, URL
  pathname change, or selected-database migration-history change occurred.

## Checklist Updates

- Task status changes: `P1-10` `[ACTIVE]` -> `[DONE]`.
- New task IDs: `P1-24`.
- Active Work row released/updated: removed the P1-10 row after durable
  selected-runtime verification.

## Next Exact Action

Claim `P1-24` alone and reconcile the canonical clean-deployment Prisma
migration lineage without rewriting or resetting the approved selected database.
