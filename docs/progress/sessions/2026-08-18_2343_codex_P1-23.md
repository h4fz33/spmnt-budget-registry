# Development Session Handoff

**Session file:** `2026-08-18_2343_codex_P1-23.md`
**Owner/agent:** `codex/root`
**Primary task:** `P1-23`
**Secondary tasks:** None
**Started:** `2026-08-18T22:39:00+07:00`
**Ended:** `2026-08-18T23:43:00+07:00`
**Outcome:** Blocked

## Intent

Establish the Product Owner-approved, dedicated non-production Cloud Run
verification runner and prove the reachable `schoolbanchee_test` migration,
idempotent seed, and focused-test sequence without retrieving, exposing, or
logging the database URI, credentials, or database content.

## Work Completed

- Retained the dedicated P1-23 Cloud Run Job, its distinct least-privilege
  runtime service account, and Secret Manager `DATABASE_URL` injection
  boundary. The existing API-token-only `d11-prisma-runner` was not changed.
- Kept the runner to test-mode migration deployment, idempotent seed, and the
  named P1-03, P1-07, P1-08, and P1-22 verification commands. Its program
  suppresses child-process output and emits only normalized check results.
- Added Debian Bookworm OpenSSL and CA certificate prerequisites before npm
  dependency installation and Prisma client generation. Cloud Build succeeded
  for image tag `20260818231000`, and the P1-23 Job was updated to that image.
- Started one authorized execution. It reached the controlled test runtime
  guard and emitted only `CHECK=MIGRATE;RESULT=FAIL;REASON=TEST_DATABASE_NAME_INVALID`.
  No database connection, migration, seed, reset, test, secret retrieval, or
  secret/database payload output occurred.

## Files Changed

| Path | Change and reason |
| --- | --- |
| `deploy/p1-23/Dockerfile` | Install OpenSSL and CA certificates so Prisma can detect the Bookworm OpenSSL runtime. |
| `DEVELOPMENT-CHECKLIST.md` | Record the exact P1-23 result under `BLK-015`, block P1-23, and release Active Work. |
| `docs/progress/sessions/2026-08-18_2343_codex_P1-23.md` | Preserve this redacted runner evidence and exact external next action. |

## Verification

| Command/check | Result | Evidence or relevant output |
| --- | --- | --- |
| Cloud Build for image `20260818231000` | PASS | OpenSSL-enabled runner image built successfully. |
| Cloud Run Job image update | PASS | The dedicated P1-23 Job accepted the new immutable image; its existing service account, test-mode variables, and Secret Manager reference were retained. |
| One controlled Cloud Run execution | BLOCKED | Normalized output: `CHECK=MIGRATE;RESULT=FAIL;REASON=TEST_DATABASE_NAME_INVALID`. The container started successfully, so the prior OpenSSL detection blocker is not the failing boundary. |
| Secret/database disclosure review | PASS | No secret payload, URI, credential, database content, raw child-process output, or reset command was retrieved, recorded, or emitted. |
| `git diff --check` | PASS | No whitespace errors. |

## Domain and Architecture Decisions

- No domain or architecture decision changed. ADR-0004's Prisma/PostgreSQL and
  migration-based persistence boundary remains authoritative.
- The Cloud Run Job is verification infrastructure only. Its successful image
  build and startup do not establish provider capability, production access,
  recovery, backup/PITR, encryption, or real-data authorization.

## Blockers and Risks

- `BLK-015` remains open. The injected secret's URI fails the existing
  fail-closed requirement that a test-mode database name contain `test`; it
  therefore cannot be used for `schoolbanchee_test` verification.
- The Secret Manager custodian must publish an approved new `latest` version
  for `SPMNT-ACC-AUDIT_PRISMA-POSTGRES-URI` that targets the isolated
  `schoolbanchee_test` database. Do not retrieve or disclose the URI locally,
  and do not reset the database.

## Checklist Updates

- Task status changes: `P1-23` `[ACTIVE]` -> `[BLOCKED]`.
- New task IDs: None.
- Active Work row released/updated: Removed the P1-23 row after recording the
  controlled execution result.

## Next Exact Action

The Secret Manager custodian must publish the approved test-database URI as a
new `latest` secret version. Then reclaim `P1-23`, execute the unchanged Cloud
Run Job once, and inspect only its normalized `CHECK`/`VERIFY_RUN` result
lines before deciding task completion.
