# Development Session Handoff

**Session file:** `2026-08-20_2230_codex_P1-20.md`
**Owner/agent:** `codex/p1-20`
**Primary task:** `P1-20`
**Secondary tasks:** None
**Started:** `2026-08-20T21:35:00+07:00`
**Ended:** `2026-08-20T22:30:00+07:00`
**Outcome:** Blocked

## Intent

Implement the bounded `P1-20` Policy Publisher designation and atomic
replacement path. A fresh-authenticated System Admin must apply only the
exact Private Business / Product Owner-approved synthetic pilot evidence,
with one current holder and one distinct standby for the fixed 17-School
scope. This is private-product testing configuration, not an external
appointment or production approval.

## Work Completed

- Added immutable Product Owner approval and System Admin execution
  provenance for a current/standby designation pair.
- Added the `AUTH-08` service: current Internal Audit evidence must be HTTPS
  on the official SESAO host, no more than 30 days old, identify the exact
  subject, and report `NO_CONFLICT`; the Product Owner approval evidence,
  current/standby identities, 17-School scope, and System Admin executor are
  all exact-match checked.
- Added serialized designation/replacement with replay, self-grant, shared
  account, stale evidence, delegation, generic grant, and concurrent-holder
  denial. Replacement activates the former standby and supersedes both
  previous designations atomically.
- Made Policy Version publication fail closed unless its current designation
  has P1-20 provenance. The P1-07 integration test now proves an older
  current-only designation cannot publish before exact P1-20 designation.
- Added focused P1-20 integration/structural verification, package commands,
  and controlled P1-23 runner wiring.

## Files Changed

| Path | Change and reason |
| --- | --- |
| `prisma/schema.prisma` | Add the P1-20 designation-provenance model and relations. |
| `prisma/migrations/20260820213000_p1_20_policy_publisher_designation/migration.sql` | Add immutable provenance, deferred database checks, and audit evidence requirement. |
| `src/lib/authorization/policy-publisher.ts` | Implement exact designation/replacement and the fail-closed resolver. |
| `src/lib/policy/core.ts` | Require P1-20 provenance before policy publication. |
| `tests/integration/p1-20-policy-publisher.test.ts` | Cover designation, replacement, stale evidence, replay, and immutability. |
| `tests/integration/p1-07-policy-versions.test.ts` | Add P1-20 provenance regression coverage for policy publication. |
| `scripts/verify-p1-20.mjs` | Verify durable designation provenance and audit structure. |
| `package.json` | Add focused P1-20 test and verifier commands. |
| `deploy/p1-23/run-verification.mjs` | Add ordered P1-20 checks to the controlled runner. |

## Verification

| Command/check | Result | Evidence or relevant output |
| --- | --- | --- |
| `npx prisma validate` | PASS | Prisma schema is valid. |
| `npx tsc --noEmit` | PASS | Completed with no diagnostics. |
| `git diff --check` | PASS | No whitespace errors; existing CRLF warnings only. |
| `npm run db:migrate:test` | BLOCKED | Approved hosted synthetic test endpoint returned `P1001` for `db.prisma.io:5432`. |
| Local isolated synthetic migration replay | BLOCKED | Sandbox denied Prisma schema-engine spawn; an elevated local-container inspection was rejected by the platform before execution. |
| `npm run test:p1-20` / `npm run verify:p1-20` | NOT RUN | Require a successfully migrated isolated synthetic database. |

## Domain and Architecture Decisions

- Policy Publisher remains an application capability, not a role inferred from
  a title, external page, or membership.
- Private Business / Product Owner approval and System Admin execution are
  retained as distinct, attributable evidence.
- The 17-School scope is a synthetic testing boundary, not authority to
  process real School data or a production deployment commitment.

## Blockers and Risks

- `P1-20` cannot be marked done until migration, focused integration, and
  structural verification run against an isolated synthetic PostgreSQL
  runtime. This is independent of `P1-13` / `BLK-014`.
- No provider, production, real School, or personal data was used or changed.

## Checklist Updates

- Task status: `P1-20` `[ACTIVE]` -> `[BLOCKED]`.
- New task IDs: None.
- Active Work row released: Yes, alongside `P1-18` after static validation.

## Next Exact Action

Restore isolated synthetic PostgreSQL migration access, then run `npm run
db:migrate:test`, `npm run test:p1-20`, and `npm run verify:p1-20` before
returning `P1-20` to `[ACTIVE]` or marking it `[DONE]`.
