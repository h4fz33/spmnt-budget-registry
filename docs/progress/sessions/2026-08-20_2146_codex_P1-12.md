# Development Session Handoff

**Session file:** `2026-08-20_2146_codex_P1-12.md`
**Owner/agent:** `codex/root`
**Primary task:** `P1-12`
**Secondary tasks:** `P1-13`
**Started:** `2026-08-20T20:20:00+07:00`
**Ended:** `2026-08-20T21:46:00+07:00`
**Outcome:** Completed

## Intent

Complete the Product Owner-authorized P1-12 test-only local PostgreSQL backup/
restore smoke test using synthetic data. Attempt the separately authorized
P1-13 public-test visibility and branch-protection configuration without
implying production authorization.

## Work Completed

- Added `test:p1-12` and a fail-closed local synthetic `pg_dump`/`pg_restore`
  procedure. It accepts only `APP_ENV=test`, `NODE_ENV=test`, a local source
  host, and a database name containing `test`; it strips Prisma-only URL query
  parameters before invoking PostgreSQL tools.
- The procedure creates a custom archive, restores to a unique clean local
  database, then compares public-table row totals, column catalog, validated
  constraint/FK catalog, public-index definitions, invalid-index count, and
  the P1-03 seed marker. PostgreSQL can canonically rewrite equivalent check
  expressions during restore, so the verifier compares the stable validated
  constraint catalog rather than DDL display text.
- Verified the existing direct local restore from the synthetic source. No
  selected Prisma runtime, Secret Manager value, hosted database, real data,
  provider configuration, or production resource was read or changed.
- Retained hosted CI evidence for P1-13. The GitHub API could not be used from
  this environment: the ordinary request failed TLS and the platform rejected
  the elevated request before execution. No repository visibility or branch
  protection state was changed or claimed as verified.
- The exact temporary restore database was dropped. The temporary archive
  directory could not be deleted because the platform rejected the destructive
  cleanup call before execution; it contains only the synthetic archive and a
  zero-byte failed-attempt file, with no credentials or real data.
- Added `/.tmp-p1-12/` to `.gitignore` so the remaining local artifact cannot
  be committed accidentally.

## Files Changed

| Path | Change and reason |
| --- | --- |
| `scripts/p1-12-backup-restore.mjs` | Added the repeatable local synthetic dump/restore and structural-integrity verifier. |
| `package.json` | Added `test:p1-12`. |
| `.gitignore` | Ignored the exact temporary P1-12 archive directory after platform cleanup rejection. |
| `DEVELOPMENT-CHECKLIST.md` | Marked P1-12 done, P1-13 blocked, released both Active Work rows, and reconciled BLK-008/BLK-014. |
| `docs/governance/p0-08-production-infrastructure-boundary.md` | Recorded the limited P1-12 proof without altering provider or production claims. |

## Verification

| Command/check | Result | Evidence or relevant output |
| --- | --- | --- |
| Direct local `pg_dump --format=custom --no-owner --no-privileges` and `pg_restore --exit-on-error --no-owner --no-privileges` | PASS | Synthetic source restored to a clean local database; archive size `205906` bytes. |
| `pg_restore --list` archive read | PASS | Archive read exit `0`; SHA-256 `CA63D4AE6E6A2C3222687B5F472B3AAD967BA130E2E41F7216A4AA869320F3F6`. |
| Source-versus-restore catalog and exact row comparison | PASS | 26 public tables, 36 rows, 56 validated foreign keys, 395 validated constraints, 96 public indexes, 0 invalid public indexes; structural fingerprint `e2e2dbef78c9f7afcdd9c01e7a30242434d681116e871c8e0dadf82ba6285803`; seed marker `P1-03-SYNTHETIC`. |
| `npm run test:p1-12` | BLOCKED before execution | The platform rejected the elevated child-process run before it started; the equivalent direct local dump/restore and read-only comparison passed above. |
| `node --check scripts/p1-12-backup-restore.mjs` | PASS | Exit `0`. |
| `npx prisma format --check` | PASS | All Prisma files formatted correctly. |
| `git diff --check` | PASS | No whitespace errors after final documentation reconciliation. |
| GitHub repository/branch API read and mutation attempt | BLOCKED | Normal request failed TLS; elevated request was rejected by the execution platform before it ran. Visibility/protection are unverified. |

## Domain and Architecture Decisions

- Product Owner authorizes only a test-only local synthetic `pg_dump`/
  `pg_restore` procedure for P1-12.
- This proof is not provider-managed backup/PITR, a scheduled backup service,
  a protected-retention design, selected-provider recovery evidence, an RPO/RTO
  drill, production readiness, or permission to process real School data.
- Public repository visibility, once externally configured for current testing,
  remains distinct from production authorization and `P5-12`.

## Blockers and Risks

- `BLK-014` remains open for P1-13. A repository administrator must make
  `h4fz33/spmnt-budget-registry` public for testing and configure `P01_dev`
  branch protection requiring `Quality`, `PostgreSQL integration`, and
  `Production build`; the resulting API state must be verified.
- P1-12 does not remove the remaining P0-08/P5-05 provider, retention,
  encryption, recovery-target, or production gates.
- Manual cleanup remains for the exact `.tmp-p1-12` directory because the
  platform rejected its destructive removal call before execution.

## Checklist Updates

- Task status changes: P1-12 `[ACTIVE]` -> `[DONE]`; P1-13 `[ACTIVE]` -> `[BLOCKED]`.
- New task IDs: None.
- Active Work row released/updated: Both P1-12 and P1-13 rows removed.

## Next Exact Action

The repository administrator must configure the authorized public-test
visibility and `P01_dev` branch protection, then reclaim `P1-13` and verify the
three required GitHub check contexts through the GitHub API.
